# 재실행 관문 증거 (2026-08-18 01:38–09:05)

연구원 판정(2026-08-17)의 스모크 관문 3단언 통과 기록.

- **재학습 20회 전부 단언 통과 · GATE-FAIL 0건 · 고유 클러스터 10종**
- (a) 정규화: 체크포인트 stats == 자기 학습 데이터(episodes[:n]) 재산출값 (l2 <= 1e-6)
- (b) 스텝: 지정값(스모크 축소 2,500) 일치 — 본실행 배치 등가값(10,000/28,000)은
      `assert_retrain_contract()`가 재학습마다 런타임 검증
- (c) |B_k| == HDF5 meta 성공 수 == episodes 그룹 수 (3중 대조)
- warm-start 미사용(scratch) 20/20 확인

**주의 1**: 본 실행은 `--n 50`이 누락되어 스모크가 4,000 ep 규모로 돌았다(실행측 실수).
관문 판정에는 영향이 없으나(단언은 재학습마다 독립 검증) 7시간 25분의 GPU가 소모됐고,
3,151 ep 지점에서 중단했다. 스트림 산출물은 관문 증거로만 보존하며 **판독에 인용하지 않는다**.

**주의 2**: 본 스모크에서 성숙 전이(I→M)는 0건이었다. 스모크 축소 상수(2,500 스텝)로는
probe 10/10을 넘기는 정책이 나오지 않기 때문이며, 본실행 상수(10,000/28,000)와 무관하다.
lifecycle 전이 검증은 2026-08-17 스모크에서 이미 통과했다(4상태 전이 전 경로 관측).
