# ★ 무효 데이터 — 인용 금지

이 디렉터리의 `stream_0.jsonl`은 **학습 데이터 출처 오류로 무효화된 실행**의 산출물이다.
결과로 인용하지 않는다. 감사 추적을 위해 지우지 않고 보존한다.

- 무효 사유와 경위: `src/log.md` 2026-08-17 항목
- 인용 금지 규칙 전체: `PROJECT_STORY.md` §7
- 형제 디렉터리 `../seed0_normstats_invalid/`는 정규화 결함으로 무효화된 별개 실행이다.

파일명에 `INVALID_` 접두사가 없으므로, 글롭 패턴으로 `results/e5/**/stream_*.jsonl`을
수집하는 스크립트는 이 파일을 걸러내야 한다.
